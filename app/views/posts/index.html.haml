#content-container
  #breadcrumb
    News
    #view-as-grid
      = link_to posts_grid_path do
        = image_tag("grid.gif")
        view as grid

  #news
    - @posts.each do |post|
      = render post
